
import 'package:flutter/material.dart';

const cardBackgroundColor = Color(0xCBFFF3);
const primaryColor = Color(0x00062a48);
const secondaryColor = Color(0xFFFFFFFF);
const backgroundColor = Color(0xFF15131C);
const selectionColor = Color(0xFF88B2AC);

const stackedColor=Color.fromRGBO(64, 64, 64, 0.32);
const primaryColorBackground = Color.fromRGBO(203, 255, 243, 1);

const defaultPadding = 20.0;

const headingFont=40.0;
const subHeadingFont=25.0;
